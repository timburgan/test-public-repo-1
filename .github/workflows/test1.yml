name: Test1

on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: run it
        run: |
          curl --request POST \
            --data '{"title": "smoke test1", "body": "smoke test1"}' \
            --header 'authorization: Bearer ${{ secrets.GITHUB_TOKEN }}' \
            --header 'content-type: application/json' \
            --url https://api.github.com/repos/timburganec/test-public-repo-1/issues
            
